#include <iostream>
#include <string>
#include <vector>
#include "dDE.h"

std::string fonction;

int main()
{
    while (true == true)
    {
      
        std::getline (std::cin,fonction);
         
        dE(fonction);

        std::cout << "slt "<< nb[0] << std::endl ;
        
        std::cout << "string " << operateur[0] << std::endl ;
    }
}